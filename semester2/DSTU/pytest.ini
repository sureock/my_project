[pytest]
DJANGO_SETTINGS_MODULE = DSTU.settings
python_files = tests.py test_*.py *_tests.py
addopts = 
    -v
    --tb=short
    --strict-markers
    -p no:warnings
markers =
    slow: marks tests as slow
    integration: integration tests